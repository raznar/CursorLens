import { DashboardPageSkeleton } from "@/components/dashboard/dashboard-page-skeleton";

/** Instant feedback during client-side route transitions. */
export default function Loading() {
  return <DashboardPageSkeleton />;
}
